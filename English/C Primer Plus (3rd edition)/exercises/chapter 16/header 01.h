#ifndef CH16_EX01_H
#define CH16_EX01_H

#define TRUE 1
#define FALSE 0
#define MAX(x, y) ((x) > (y) ? (x) : (y))
#define MIN(x, y) ((x) > (y) ? (y) : (x))
#define ABS(x) ((x) > 0 ? (x) : -(x))
#define BOOLEAN int

#endif